{ parameter (or (or (int %decrement) (int %increment)) (unit %reset)) ;
  storage int ;
  code { UNPAIR ;
         IF_LEFT
           { IF_LEFT
               { SWAP ; SUB }
               { SWAP ; PUSH nat 1 ; PUSH nat 1 ; ADD ; DROP ; ADD } }
           { DROP 2 ; PUSH int 0 } ;
         NIL operation ;
         PAIR } }

